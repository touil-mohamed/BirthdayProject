name: Full Stack CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend:
    name: Backend CI/CD
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./BirthdayApi
    env:
       DB_HOST: ${{ vars.DB_HOST }}
       DB_USER: ${{ vars.DB_USER }}
       DB_PASSWORD: ${{ vars.DB_PASSWORD }}
       DB_PORT: ${{ vars.DB_PORT }}
       DB_DATABASE: ${{ vars.DB_DATABASE }}
    steps:
    - uses: actions/checkout@v2

    - name: Setup Node.js
      uses: actions/setup-node@v1
      with:
        node-version: '18'

    - name: Utiliser les variables d'environnement
      run: |
          echo "Hôte de la base de données : $DB_HOST"
          echo "Nom d'utilisateur de la base de données : $DB_USER"
          echo "Mot de passe de la base de données : $DB_PASSWORD"
          echo "Port de la base de données : $DB_PORT"
          echo "Nom de la base de données : $DB_DATABASE"

  #   - name: Install Backend Dependencies
  #     run: npm install

  #   - name: Run Backend Lint
  #     run: npm run lint

  #   - name: Run Backend Tests
  #     run: npm test

  #   - name: Run Husky Git Hooks
  #     run: npm run prepare

  #   - name: Run Commitlint
  #     run: npm run commitlint

  #   - name: Run Audit
  #     run: npm run audit
    
    

  #   # - name: Check for vulnerabilities in Backend Dependencies
  #   #   run: npm audit

  #   # Add additional backend steps as needed (Docker image build, security scans, etc.)

  # frontend:
  #   name: Frontend CI/CD
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./front_end
  #   steps:
  #   - uses: actions/checkout@v2

  #   - name: Setup Node.js
  #     uses: actions/setup-node@v1
  #     with:
  #       node-version: '14'

  #   - name: Install Frontend Dependencies
  #     run: npm install

  #   - name: Run Frontend Lint
  #     run: npm run lint

    #- name: Run Frontend Tests
    #  run: npm test

    # - name: Build Frontend
    #   run: npm run build

  #   Add additional frontend steps as needed (deployment, etc.)

  # Add additional jobs for Docker checks, secret scanning, etc.